<h1>게시판</h1>
<a href='/post/new'>[새 글 쓰기]</a><br><br>

<% @posts.each do |post| %><br>
    제목 : <%= post.title %><br>
    내용 : <%= post.content %><br>
    <a href='/post/edit/<%=post.id%>'>[수정]</a>
    <form action='/post/destroy/<%= post.id %>' method='post'>
        <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
        <input type='submit' value='[삭제]'></input>
    </form>
<% end %>